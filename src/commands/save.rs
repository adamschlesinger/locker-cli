use crate::commands::{ Save, CLICommand };
use crate::lfs::*;

impl CLICommand for Save {
    fn exec(&self) {

        // check for changes

        // if not in claim branch, make claim branch

        // commit work

        // push changes
    }
}